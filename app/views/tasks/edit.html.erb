<h1>editing task</h1>

<%= form_for @task, method: :patch do |f| %>
<p>
		<%= f.label :description %>
		<%= f.text_field :description %>
	</p>
	<p>
		<%= f.submit %>
	</p>

<% end %>

<%= link_to 'Back', tasks_path %>